| 算法名称 | 算法种类 | 算法功能 | 更新时间 | 贡献人 | 仍存在问题 |
| --- | --- | --- | --- | --- | --- |
| `feels_like_temperature` / 体感温度 | `02diagnostic` | 根据气温、10 米风速、相对湿度和气压综合风寒与显温，计算体感温度诊断场 | 2026-08-17 | 郭云谦、王亭波 | 原目录 pytest 14 passed，中间目录 12 passed / 2 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；`test_data/` 未同步；`resource/` 为空 |
| `orographic_temperature_downscaling` / 气温降尺度(地形) | `00space_downscale` | 基于层结递减率和地形高度差进行气温空间降尺度与地形订正 | 2026-08-17 | 郭云谦、王亭波 | 中间目录导入已统一为 `orographic_temperature_downscaling`（体感温度已拆出）；2026-08-17 增量同步：改用 `meb` 直接调用、简化 `utils`、补齐 `preprocess_test_data.py`；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；同步后尚未重跑 pytest；`test_data/` 由独立仓库存放；`resource/` 为空 |
| `orographic_wind_downscaling` / 风降尺度(地形) | `00space_downscale` | 基于地形粗糙度、地形高度标准差和地形高度差进行风速空间降尺度与订正；支持投影米制与真经纬分辨率推断 | 2026-08-21 | 郭云谦、王亭波 | 已按原目录增量同步（含经纬分辨率推断、预处理 CLI、新增测试）；中间目录导入已统一为 `orographic_wind_downscaling`；说明与 notebook 对齐原目录 `wind_calculations.ipynb`；原目录 pytest 32 passed，中间目录 30 passed / 2 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；`test_data/` 未同步；`resource/` 为空 |
| `neighbourhood_probability_processing` / 邻域(nbhood)处理及概率生成 | `07probability` | 进行邻域概率处理、邻域百分位生成、陆海/地形带分区和分层掩码概率处理 | 2026-08-24 | 郭云谦、王亭波 | 中间目录导入已统一为 `neighbourhood_probability_processing`；2026-08-24 增量同步：改用 `meb.checkout_griddata`、补齐 `preprocess_test_data.py`、删除包内脚手架与原目录已移除的 `cli/io.py`；原目录 pytest 53 passed，中间目录 42 passed / 11 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 10.89MB 未同步待筛选；`resource/` 为空 |
| `threshold_probability_conversion` / 阈值概率转换 | `07probability` | 将诊断场转为相对阈值的 0～1 真值/概率场；支持硬阈值、fuzzy、多阈值、单位换算、集合/时间压维和 vicinity 邻域最大值 | 2026-08-24 | 郭云谦、王亭波 | 中间目录导入已统一为 `threshold_probability_conversion`；首次整理：同步原目录并改依赖为 `neighbourhood_probability_processing`；原目录 pytest 41 passed，中间目录 27 passed / 14 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 5.88MB 未同步待筛选；`resource/` 为空 |
| `orographic_precipitation_downscaling` / 降水降尺度(地形) | `00space_downscale` | 基于温湿压、风场和地形抬升效应计算降水地形增强项，并支持叠加或扣除到降水场 | 2026-08-18 | 郭云谦、王亭波 | 中间目录导入已统一为 `orographic_precipitation_downscaling`；原目录 pytest 已通过；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例在 `NIMM_pip_testdata`（含 CLI 输出）待筛选；`resource/` 为空 |
| `multi_rain_mait01_blending` / 逐小时多源自适应降水集成MAIT01 | `05blending` | 基于多模式站点降水、历史实况和当前样本计算 TS 动态权重，完成逐小时多源降水融合、频率匹配和站点到格点产品生成 | 2026-06-29 | 郭云谦、曹勇、陈荣 | 缺少 `utils/mai_1_plugin_context.py` 源码和 `resource/para_1_background.ini`；资源文件较大；导入路径待正式入库时调整；尚未运行完整测试 |
| `multi_wind_fft_blending` / 多风场FFT融合 | `05blending` | 基于FFT谱方法计算主风场与多个辅助风场的特征位移场，实现多风场特征匹配融合；`process` 可模块调度并支持 `is_multi` | 2026-07-23 | 胡海川、李振、郭云谦 | `src/main.py`（`process` / `__main__` 传参）+ `python -m cli`；详日志 `00log/multi_wind_fft_blending.md`、算法包 `00log/fft_merge_整理_20260723.log`；`NIMM_pip_testdata/.../test_data` 样例缺失需补齐；尚未迁入正式 `NIMM/05blending/` |
| `emos_probability_calibration` / emos概率订正 | `07probability` | 基于 EMOS 训练集合预报订正系数，并输出订正后的集合、概率或分位数预报 | 2026-06-29 | 郭云谦、王亭波 | 导入路径仍为原始 `src`/`utils` 包名；原始 demo 依赖 `iris` 和 `improver`；算法为 spot-only；尚未运行完整测试 |
| `multi_rain_mait24_blending` / 逐日多源自适应降水集成MAIT24 | `05blending` | 基于分区 TS 评分、beta 历史记忆和频率匹配进行 24 小时多源降水自适应融合，并输出站点和格点产品 | 2026-06-29 | 郭云谦、曹勇、陈荣 | 导入路径仍为原始 `src`/`utils` 风格；依赖完整 Micaps 业务数据环境；resource 需筛选最小资源集；尚未运行完整测试 |
| `kalman_element_forecast_correction` / kalman滤波要素预报订正 | `04single_calibration` | 基于 Kalman 平均误差场更新和预报订正，对 SWVL/STL 等要素预报进行单模式偏差订正 | 2026-08-17 | 郭云谦、曹勇、陈荣 | 已同步用户更新代码，正式代码导入路径已调整；仍依赖生产数据路径和 meteva_base 网格数据；缺少独立 NetCDF 测试数据；尚未运行完整业务测试 |
| `fy4b_argi_l1_decoder` / FY4卫星ARGI的L1数据解码 | `basic_data` | 读取 FY-4B AGRI L1 HDF 通道数据，完成定标、静止卫星投影到规则经纬度网格重采样，并按通道输出 NetCDF | 2026-06-29 | 郭云谦、丰硕 | 原始目录未提供真实 HDF 样例数据；算法名使用 `argi` 而源码包名保留 `fy4agri`; 正式入库时需调整导入路径 |
| `linear_scd_multi_time_fusion` / 线性SCD多时效融合 | `05mulit_integrate` | 将短临 `unet_qpf` 与模式 `mait_st` 拆分到统一 10 分钟网格，并按关键时效权重进行线性或显著性 SCD 融合及结果补齐 | 2026-06-29 | 郭云谦、丰硕 | 导入路径仍为原始 `nimm_scd` 包名；配置保留生产业务路径且融合配置默认 `dry_run = false`；缺少独立小样例 NetCDF/MICAPS 测试数据；尚未运行完整业务测试 |
| `orographic_nwp_3d_downscaling` / 气温地形降尺度(三维插值) | `00space_downscale` | 基于模式场、地形、站点或格点配置执行气温等近地面要素的快速精细化三维插值，并支持最优参数求解 | 2026-08-17 | 曾晓青、丰硕、赵如奇 | 已同步用户更新代码，正式代码导入路径已调整；仍依赖真实业务数据、`Parameter/lib` 目录、`eccodes` 环境和二进制扩展 `Module_Micaps_RW.so`；缺少独立端到端测试数据，尚未运行完整业务测试 |
| `frequency_matching_rain24` / 逐日降水频率匹配订正 | `04single_calibration` | 基于历史相似个例、光流位移订正和频率匹配，对数值模式 24 小时累积降水预报进行站点和格点订正 | 2026-06-29 | 郭云谦、陈荣、李东勇 | 尚未封装统一插件类和 `process()` 方法；依赖 `meteva`、真实业务数据和生产路径配置；`src/verify_data/` 暂按原结构保留；尚未运行完整业务测试 |
| `steps_multi_time_fusion` / STEPS多时效融合 | `05mulit_integrate` | 基于 STEPS cascade 对 nowcast、NWP、随机噪声和气候态 skill 进行多时效降水融合 | 2026-06-29 | 郭云谦、丰硕 | 完整业务 I/O 依赖 `netCDF4`、`meteva.base`、`cartopy` 等环境；notebook 中仍可能保留旧示例路径；尚未补充到正式 `NIMM/05mulit_integrate/` 目录 |
| `frequency_matching_rain01` / 逐小时降水频率匹配订正 | `04single_calibration` | 基于历史同期模式-实况样本、TS+BIAS相似筛选、光流位移订正和CDF频率匹配，对单模式逐1小时降水预报进行统计订正并输出 MICAPS3/MICAPS4 产品 | 2026-06-29 | 郭云谦、陈荣、李东勇 | 导入路径仍为原始 `src`/`proc`/`data`/`utils` 风格；未提供标准插件类和 `process` 方法；配置保留生产或旧工程路径；完整业务测试依赖真实 MICAPS 和模式资料 |
| `optical_flow_nowcast` / 临近光流法外推预报 | `03nowcast` | 基于 Lucas-Kanade 光流估计平流速度场，并使用半拉格朗日方法进行多时效临近降水外推预报 | 2026-06-29 | 郭云谦、陈荣、丰硕 | 完整运行依赖 `scipy`、`scikit-image`、`opencv-python`、`meteva-base` 等环境；`temp_demo.py` 和 notebook 仍保留原始 `/home/nimm` 与 `nimm.cli.nimm.*` 示例路径；尚未补充到正式 `NIMM/03nowcast/` 目录 |
| `radar_qpe_retrieval` / 雷达降水定量反演QPE | `01obs_adustment` | 基于雷达反射率、KDP、比衰减和水凝物分类等网格数据，使用 Z-R、KDP-R、A-R、融合和水凝物分类方法估算降水率 | 2026-07-13 | 郭云谦、王亭波 | 中间目录导入已统一为 `radar_qpe_retrieval`（回波分类已拆出）；原目录测试通过，中间目录 32 passed / 6 skipped；正式入库需改 `NIMM` 包路径；样例在 `NIMM_pip_testdata`（体量较大）待筛选；`resource/` 为空 |
| `radar_echo_classification` / 雷达回波分类 | `01obs_adustment` | 雷达回波层状/对流分类、特征识别与半监督水凝物分类 | 2026-07-16 | 郭云谦、王亭波 | 中间目录导入已统一为 `radar_echo_classification`；原目录测试通过，中间目录 13 passed / 4 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例在 `NIMM_pip_testdata`（体量较大）待筛选；`resource/` 为空 |
| `station_grid_fusion` / 网格站点融合算法 | `08consistense` | 基于站点观测和格点背景场进行 IDW 插值、站点-格点偏差扩散和 Gaussian/IDW 融合，生成一致协调的格点产品 | 2026-06-29 | 郭云谦、陈荣 | 依赖 `nimm.PostProcessingPlugin`、`meteva_base`、`meteva` 和 `scipy`；原始示例保留业务绝对路径；`InterpSgTotal` 有硬编码 sys.path 且有背景场分支缺少 `interp_sg_idw_delta` 导入；缺少真实测试数据 |
| `radar_wind_dealiasing` / 雷达风场退模糊算法 | `01obs_adustment` | 基于区域连通关系和 Nyquist 速度区间展开，对多普勒雷达径向速度进行退模糊，并支持门点过滤、参考速度锚定和可选经纬度重映射 | 2026-07-21 | 郭云谦、王亭波 | 中间目录导入已统一为 `radar_wind_dealiasing`；原目录与中间目录 pytest 33 passed；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；`resource/` 为空；单层小样例是否入库后续决定 |
| `coordinate_projection_transform` / 坐标投影转换 | `ancillaries` | 提供经纬度网格、Lambert 等距投影网格、Iris Cube 和 meteva 格点数据之间的坐标投影转换与结构转换 | 2026-06-29 | 郭云谦、陈荣 | 依赖上层 `nimm` 包结构和 `iris`、`cartopy`、`improver`、`meteva_base` 等环境；原始 notebook 体量较大且可能含旧业务路径；`resource/` 当前为空；尚未运行完整业务测试 |
| `generate_ancillary` / 地形辅助场生成 | `ancillaries` | 对插值后海陆掩码做 0/1 二值化纠正，按阈值配置将地形高度场分带输出各带二值掩码，并计算地形带折叠权重 | 2026-08-21 | 郭云谦、王亭波 | 中间目录导入已统一为 `generate_ancillary`；原目录 58 passed，中间目录 50 passed / 8 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 3.88MB 未同步待筛选；`resource/` 为空 |
| `generate_derived_solar_fields` / 太阳衍生场生成 | `ancillaries` | 按目标网格计算地方太阳时；累计指定时段晴空太阳辐射 | 2026-08-22 | 郭云谦、王亭波 | 中间目录导入已统一为 `generate_derived_solar_fields`；2026-08-22 增量同步：改用 `meb.checkout_griddata`、补齐 `preprocess_test_data.py`；已删除包内脚手架与原目录没有的 `utils/utils.py`；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 1.25MB 未同步待筛选；`resource/` 为空 |
| `generate_orographic_smoothing_coefficients` / 地形平滑系数生成 | `ancillaries` | 基于地形邻接梯度生成递归滤波 x/y 平滑系数；可选掩码置零；支持投影米制与经纬球面路径 | 2026-08-22 | 郭云谦、王亭波 | 中间目录导入已统一为 `generate_orographic_smoothing_coefficients`；2026-08-22 增量同步：改用 `meb.checkout_griddata`、补齐 `preprocess_test_data.py`；已删除包内脚手架与原目录没有的 `utils/utils.py`；原目录 pytest 19 passed，中间目录 12 passed / 7 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 2.66MB 未同步待筛选；`resource/` 为空 |
| `regrid` / 海陆感知重网格 | `ancillaries` | 将源场重网格到目标空间网格，可选海陆掩膜感知以避免陆取海值 / 海取陆值；支持双线性、最近邻及新版 `*-2` 路径 | 2026-08-17 | 郭云谦、王亭波 | 中间目录已按原目录增量同步（`meb.checkout_griddata`）；原目录 38 passed，中间目录 25 passed, 13 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；测试数据约 1.70MB 未同步待筛选；`resource/` 为空 |
| `station_height_difference_correction` / 站点高差订正 | `04single_calibration` | 当模式格点高度与站点高度不一致时，用历史站点预报、实况与邻点高差估计订正因子并应用到新预报，减弱高度差系统性偏差 | 2026-08-05 | 郭云谦、王亭波 | 模块名由 `dz_rescaling` 更名为 `station_height_difference_correction`（类名保留）；原目录 pytest 27 passed，中间目录 26 passed, 1 skipped；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 3.07MB 未同步待筛选；`resource/` 为空 |
| `probability_reliability_correction` / 概率可靠性订正 | `07probability` | 用历史概率预报与阈值化实况构建可靠性表，经聚合/整理后订正待发布概率场（网格与站点） | 2026-08-08 | 郭云谦、王亭波 | 中间目录导入已统一为 `probability_reliability_correction`；原目录与中间目录 pytest 均为 18 passed；正式入库需改 `NIMM` 包路径并评估统一 `BasePlugin`；样例约 1.32MB 未同步待筛选；`resource/` 为空 |
