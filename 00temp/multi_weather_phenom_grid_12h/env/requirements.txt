# 网格天气现象电码生成系统 (QX/T 740-2024)
# 运行环境依赖
# 安装方式: pip install -r env/requirements.txt
#           或 conda env create -f env/environment.yml

# ── 核心依赖 ──────────────────────────────────────────────────
python>=3.9

numpy>=1.23.0
xarray>=2022.6.0
netcdf4>=1.6.0

# ── 气象数据支撑库 ────────────────────────────────────────────
# meteva: 国家气象中心自研统一数据支撑库
# 文档: https://www.showdoc.com.cn/metevabase
# 安装: 请参考内网部署文档（含 meteva_base 及依赖）
meteva

# ── 可选：并发性能优化 ─────────────────────────────────────────
numexpr>=2.8.0    # 多核数值表达式加速（建议设置 NUMEXPR_MAX_THREADS）

# ── 开发/测试依赖（生产环境可不安装）────────────────────────────
pytest>=7.0.0
